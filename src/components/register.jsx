import React from "react";

const Register = () => {
    return (
        <section className="register-route">
            <h1>Register Page</h1>
        </section>
    );
};

export default Register;
